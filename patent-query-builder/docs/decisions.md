# 決定事項（実装時に確認・判断した事項の記録）

企画書 §14.4「方言仕様や CSV 列名など人に確認して決めた事項は docs/decisions.md に日付付きで追記する」に従う。

| 日付 | 事項 | 決定 | 根拠・備考 |
|---|---|---|---|
| 2026-09-14 | 依存ライブラリ | **Python 標準ライブラリのみ**で実装（numpy／pandas／scikit-learn／openpyxl も使わない） | 開発環境に Anaconda が無く、実行環境の版も未確認（Q8）。決定木・ロジスティック回帰（CAL）・xlsx 読み書きは自前実装。Anaconda 版への置換は将来の任意 |
| 2026-09-14 | 人の UI | Excel 設計シートに加えて **ローカル Web UI**（`server.py`、127.0.0.1）を主インタフェースにする | リポジトリ内の他アプリと同じ構成（ランチャーから起動可）。Excel は出力・読み戻しで併用。Jupyter ドライバは CLI（`python -m pqb`）で代替 |
| 2026-09-14 | J-PlatPat 論理式（仮） | `[語/AB+語/CL+コード/FI]*[…]`（Block を `[ ]`、OR を `+`、AND を `*`、NOT を `-`、フィールド後置 `/`、文字数上限 2000） | 企画書 付録A の例に合わせた暫定。**公式ヘルプで確認後に `config/dialects/jplatpat.json` を差し替える**（Q3）。語中のハイフンは閉じ括弧直後以外は演算子と見なさない |
| 2026-09-14 | CSV 列名（仮） | 文献番号／発明の名称／要約／請求の範囲／FI／Fターム／IPC／公知日／出願人/権利者／引用文献。多値は `;` `,` `|` 改行 | `config/csv_dialects/jplatpat.json`。実データで確認後に確定（Q2） |
| 2026-09-14 | 分類コードの書式（仮） | FI `C22C38/04,301@A`（展開記号 `,301`、分冊識別記号 `@A`）、Fターム `4K037AA01`、IPC `C22C38/00`（空白除去） | 粒度: FI subclass→main_group→subgroup→expansion→volume、FT theme→theme_view→full、IPC subclass→main_group→subgroup。分類表データ入手後に確定（Q5） |
| 2026-09-14 | 分類表辞書の縮退 | 公式データが無いときは既知文献・CSV に現れたコード（全粒度）を「観測」として既知扱い。公式データがあれば観測のみは未照合 | 企画書 §15「縮退モード」。未照合コードは要確認フラグ |
| 2026-09-14 | 既知文献の分類の扱い | 既知文献に付与されたコードをそのまま（付与された粒度で）候補にする。粗い粒度への集約は RSJ 側（§10.4）で行う | 主群を機械的に追加すると候補が膨らみ、標準案が広がり過ぎたため |
| 2026-09-14 | ADD_AXIS／DROP_AXIS の表現 | 観点の種別（必須／補助）は G1 で固定したまま、案件設定 `extra_and_axes`／`dropped_axes` で「標準・広めに AND 軸として含めるか」を切り替える | 観点の骨格を変えない（§5.4 原則 5）。狭めは常に必須＋補助 |
| 2026-09-14 | DROP_TERM の保護 | 由来が `input`（人が G1 で入れた代表語）の語は削除候補にしない | 入力語が同義語に置き換わる退化を防ぐ |
| 2026-09-14 | 変換候補の観点割当 | 既存の語との文字列重なり（2 文字以上）で割り当て、迷うものは `assigned=auto`（要確認）。Policy は auto を採用しない。式に無い補助観点への語追加は候補表（G2）側で扱う | §10.5「振り分けに迷うものは P5／人が確認」 |
| 2026-09-14 | 採点の多数決 | P4 は `overall` の多数決、観点別も多数決。P3 は (観点, 体系, コード) の集合で投票し、コードごとの支持率から フリップ率 = 1 − 支持率 | §9.4 |
| 2026-09-14 | manual モードの識別子 | `call_key = 案件:プロンプトID:入力ハッシュ12桁:サンプル番号`。同じ入力なら再実行時に貼り戻した返答を再利用する | UI／CLI／ファイル（prompts_out/in）で共通 |
| 2026-09-14 | 母集団 U | その反復の実行結果のうち、文献リストを持つ最初の案（広め → 標準 → 狭め）。無ければ前反復の結果を流用 | 件数のみ（CSV なし）の取り込みは指標の件数だけに使う |
| 2026-09-14 | xlsx | 標準ライブラリで書き出し（インライン文字列、数式なし、ヘッダ固定・太字）・読み込み（共有文字列・インライン文字列・数値・真偽） | 企画書 §9.8「列名で照合し、セル位置に依存しない。数式は使わず値のみ」 |
| 2026-09-14 | ポート | Web UI は 8740（ランチャー登録） | 他アプリ（8720/8750/8765〜8767/8770/8780/8790/8801/8802/8888）と重複しない |
| 2026-09-14 | 商用DB API の契約 | 汎用 JSON（`POST /search` ページング、`GET /documents/{id}`、`cited_by` 任意）を `docs/db_api_contract.md` に定義。ベンダ固有の差はアダプタの変換部で吸収 | Q1 未確定のため、社内の中継サーバに実装できる最小の契約にした。アクセスは `db_access` に記録し案件ごとに上限 |
| 2026-09-14 | 引用拡張の範囲 | 引用エッジは `doc_citations`（CSV の引用文献列・API の citations／cited_by）から取る。手元に無い文献は「未取得」として人の作業項目に載せ、api モードのときだけ DB から取得を試みる | J-PlatPat では人が手動で確認する経路（§10.9）。同族（ファミリ）はデータが無いため未対応 |
| 2026-09-14 | プール文献の RSJ 入力 | 既知文献だけでなく、U 外のプール文献（引用拡張で加わったもの）も適合として RSJ・決定木に含める。U 外のプール文献数を「被覆」として報告 | §10.9「分類コードを RSJ の入力に加える」「U に含まれなければ広め案の穴」 |
| 2026-09-14 | 変換探索（GEPA 接続）の範囲 | 狭める変換の合成だけを U の内側で探索（ビーム＋パレートフロント、2 手順以上、基準をパレート改善するものだけ提示）。広げる変換は DB 実行が要るため単独候補のまま | §10.6。P5 に前世代の失敗（落としたプール文献）をフィードバックして反省的変異とする |
| 2026-09-14 | 複合変換の適用 | `COMPOSITE` 操作として変換候補表に並べ、採用時は手順を順に候補表・案件設定へ反映。Policy は複合変換を 1 つだけ採用し、含まれる単独の狭める変換は二重適用しない | |
| 2026-09-14 | SDI の差分定義 | 「既知」＝当案件のこれまでの実行結果・プール・判定済み文献の和集合。差分は文献番号の比較（内容更新は検知しない）。新規は P4 で採点し上限を超えた分は未採点として報告 | 定期実行は OS のスケジューラから CLI を呼ぶ運用（アプリ内スケジューラは持たない） |
