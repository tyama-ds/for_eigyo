# 未決事項（企画書 §16 の表を引き継ぎ、実装時に追記）

| ID | 事項 | 影響範囲 | 確認先 | 状態 | 実装での暫定対応 |
|---|---|---|---|---|---|
| Q1 | 契約している商用DB の名称、API の有無と仕様、CSV エクスポートの項目・件数上限、アクセス上限 | 案Aの成立可否、DB アダプタ、方言 | 知財部門・DB ベンダ | 未 | `db.mode=api` は `docs/db_api_contract.md` の汎用 JSON 契約（ページング・文献取得・アクセス記録）を実装済み。ベンダ固有の変換は仕様確定後に追加。CSV は `config/csv_dialects/generic.json` |
| Q2 | J-PlatPat の CSV 出力に含まれる項目（分類、要約の有無）と件数上限 | 逆算・CAL の機能範囲 | 実データで確認 | 未 | 列名候補を `csv_dialects/jplatpat.json` に置き、無い列は `info.missing` として UI に表示。名称のみでも動く |
| Q3 | J-PlatPat 論理式の正確な文法（フィールド識別子、演算子、分類記法、近接、文字数上限） | レンダラ | 公式ヘルプ・操作マニュアル | 未 | `dialects/jplatpat.json` は付録A の例に合わせた仮。近接演算子は DSL に保持のみ |
| Q4 | 社内で本番利用できる LLM の経路（API の有無、モデル、送信可能な情報の範囲） | LLM アダプタ、機密ルール | IT・知財部門 | 未 | 既定は `mock`。`manual` で社内 LLM 画面を人が経由。`api` は production で送信前確認＋マスキング |
| Q5 | FI／Fターム／IPC の分類表データの入手経路と書式 | 分類表辞書、階層集約 | 特許庁提供データの確認 | 未 | `scheme,code,title,parent,level` の CSV を取り込む。無ければ縮退モード |
| Q6 | 過去案件の検索式・既知文献・最終適合文献の蓄積状況と件数 | 後方テスト、few-shot | 知財部門 | 未 | パーサで既存の式を DSL 化できる（`pqb.core.render.parse`）。後方テストの手順は `tests/test_orchestrator.py` の記録済み判断方式 |
| Q7 | 調査種別ごとの目標母集団レンジと再現率目標の妥当性 | 停止条件、選択方針 | 調査担当 | 未（値は仮） | `config/purposes.json` |
| Q8 | 実行環境の Python バージョンと Anaconda 同梱ライブラリの版、sqlite3 の FTS5 有効可否 | 依存制約、local_index | 実行環境で `python -c` により確認 | 未 | Python 3.10 以上・標準ライブラリのみで動作するよう実装。FTS5 は使っていない（local_index は DSL の機械照合） |
| Q9 | 引用・被引用・同族情報の取得手段 | 引用拡張 | Q1・Q2 に依存 | 一部対応 | CSV の「引用文献」列と API の `citations`／`cited_by` から引用エッジを作り、手元にある文献は自動で採点・プール追加。手元に無い文献は「未取得」として人の作業項目に。同族（ファミリ）は未対応 |
| Q10 | 案件データの保管場所とアクセス権（SQLite ファイルの置き場所、共有方法） | 運用 | IT・知財部門 | 未 | `data/pqb.sqlite`（環境変数 `PQB_DATA_DIR` で変更可） |
| Q11（追記） | 汎用語リスト（`pqb/stats/stopwords_ja.txt`）の初期内容 | 語候補のノイズ | 調査担当 | 未 | 初期版を同梱。G2 の棄却ログから育てる運用 |
| Q12（追記） | LLM 採点（P4）と人の適合ラベルの一致（κ ≥ 0.6）を測るデータ | 評価計画 §12 | 知財部門 | 未 | 判定テーブルに LLM／人の両方を保持しており、κ の計算はデータが揃えば可能 |
| Q13（追記） | SDI の定期実行の方式（OS のタスクスケジューラ、ランチャー、社内ジョブ基盤のどれか）と結果の通知先 | 運用 | IT・知財部門 | 未 | CLI `python -m pqb sdi` を外部から呼ぶ前提。アプリ内スケジューラは持たない |
