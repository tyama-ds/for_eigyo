次の文献が、固定された検索観点にどれだけ合致するかを採点してください。
直前の反復の結果や他の文献との類似ではなく、**観点定義との一致**だけを見ます。

# 検索観点（定義と語）
[[axes]]

# 総合の規則
[[overall_rule]]

# 文献
[[doc]]

# 指示
- 観点ごとに 0〜3（0 無関係、1 周辺、2 一部合致、3 合致）。
- overall は上記規則（既定: 必須観点の一致度の最小値）で 0〜3。
- rationale は根拠の一文。要約が無い場合は名称だけで判断し、low_confidence を true にする。

# 出力（この JSON スキーマ以外の出力は禁止。日本語）
```json
{"per_axis": {"A": 3, "B": 2}, "overall": 2, "rationale": "…", "low_confidence": false}
```
