検索式の根拠レポートのために、各語・各分類の採用理由を文章化してください。
**数値はシステム側の値をそのまま用い、新たに生成しない**こと。

# 検索観点
[[axes]]

# 採用された語・分類とその統計・判断ログ
[[candidates]]

# 出力（この JSON スキーマ以外の出力は禁止。日本語）
```json
{"explanations": [{"kind": "term", "axis_id": "A", "value": "高張力鋼板", "text": "…"}],
 "summary": "…", "low_confidence": false}
```
