{
  "type": "object",
  "required": ["terms"],
  "properties": {
    "terms": {"type": "array", "items": {
      "type": "object", "required": ["axis_id", "text", "variant_kind", "origin"],
      "properties": {
        "axis_id": {"type": "string"},
        "text": {"type": "string", "minLength": 1},
        "variant_kind": {"type": "string", "enum": ["original", "synonym", "variant", "broader", "narrower", "abbreviation", "english", "extracted"]},
        "origin": {"type": "string"},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1}
      }}},
    "low_confidence": {"type": "boolean"}
  }
}
