{
  "type": "object",
  "required": ["axes"],
  "properties": {
    "axes": {"type": "array", "minItems": 1, "maxItems": 8, "items": {
      "type": "object", "required": ["axis_id", "name", "kind", "definition"],
      "properties": {
        "axis_id": {"type": "string", "minLength": 1},
        "name": {"type": "string", "minLength": 1},
        "kind": {"type": "string", "enum": ["required", "auxiliary"]},
        "category": {"type": "string", "enum": ["problem", "structure", "effect", "use", "other"]},
        "definition": {"type": "string"},
        "evidence": {"type": "string"},
        "terms": {"type": "array", "items": {"type": "string"}}
      }}},
    "notes": {"type": "string"},
    "low_confidence": {"type": "boolean"}
  }
}
