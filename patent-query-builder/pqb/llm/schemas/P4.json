{
  "type": "object",
  "required": ["per_axis", "overall", "rationale"],
  "properties": {
    "per_axis": {"type": "object", "patternProperties": {".*": {"type": "integer", "minimum": 0, "maximum": 3}}},
    "overall": {"type": "integer", "minimum": 0, "maximum": 3},
    "rationale": {"type": "string"},
    "low_confidence": {"type": "boolean"}
  }
}
